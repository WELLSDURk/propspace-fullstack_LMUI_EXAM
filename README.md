



This contains everything you need to run your app locally.in ( http://localhost:3000/)

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2.
3. Run the app:
   `npm run dev`
